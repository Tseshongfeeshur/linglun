# 伶伦项目协作规则

## 项目概况

伶伦是一个使用 Flutter 编写的本地音乐播放器，当前阶段主要面向 Linux 桌面环境，同时保留跨平台扩展能力。

## 原始项目目标

- 项目根目录由 Flutter 初始化，当前阶段以 Linux 桌面端为主要目标。
- 伶伦需要逐步集成美观、现代、流畅的播放页歌词动画、背景动效和歌曲陈列样式。
- 音频处理需要提供较丰富的 EQ 和高质量 DSP 能力，包括科学均衡器、智能 Bass 增强、耳机空间感处理，以及可选的上混或其他缓解头中效应的处理方式。
- DSP 实现应优先保证音质、稳定性、可解释性和可调参数范围。对于复杂算法，如果自行实现难以达到可靠质量，可以调研并借鉴优秀开源项目的算法，但必须遵守其许可证并在文档中记录来源和改动。
- 播放器应保持本地优先，不依赖在线服务才能完成核心播放、元数据、歌词和音频处理功能。

## 语言要求

- 新增或修改的代码注释必须使用简体中文。
- 与用户的对话、项目文档、提交说明和界面文案必须使用简体中文，除非存在明确的技术名称或协议名称。
- 注释应解释设计原因、协议差异和不明显的行为，不要添加描述显而易见代码的空洞注释。

## Git 规则

- 任何 Git 命令都必须先通知用户，由用户自行执行。
- 不得执行 `git status`、`git diff`、`git add`、`git commit`、`git reset`、`git checkout` 等 Git 命令。
- 不得删除、覆盖或回滚用户已有的修改。
- 需要用户执行 Git 操作时，应给出明确、可直接复制的命令，并说明操作目的。

## 开发规则

- 修改前先阅读相关模块和现有实现，优先沿用项目已有的架构、依赖和数据流。
- 手工编辑文件必须使用 `apply_patch`，不要使用 shell 重定向、`cat` 或脚本直接覆盖源文件。
- 默认使用 ASCII 编辑文件；只有项目已有内容或功能确实需要时才引入其他字符集。
- 保持修改范围聚焦，不进行与当前任务无关的重构或格式化。
- 新增依赖前先检查 `pubspec.yaml` 和现有依赖，避免重复引入功能相同的库。
- 对用户可见功能补充相应测试，尤其是解析器、音频元数据、播放状态和跨模块数据转换。

## Flutter 规则

- 使用现有 Flutter、Dart 和 Riverpod 版本及项目既有模式。
- 修改 Dart 文件后运行 `dart format` 和 `dart analyze`。
- 涉及用户交互时，检查 Linux 桌面窗口尺寸、鼠标悬停、拖动、键盘焦点和动画状态。
- 不要把复杂业务逻辑放进 Widget；解析、音频处理、数据库和播放控制应保留在对应的领域或应用层。
- UI 文案、错误提示和无数据占位文本使用简体中文。

## 歌词解析规则

- 原始歌词格式必须先转换为统一领域模型，渲染层不得直接解析原始歌词文本。
- 统一支持无时间戳、逐行时间戳和逐字时间戳三种数据结构。
- 保留翻译、对唱、说话者、语言和逐字时间等扩展信息。
- 解析器需要兼容内嵌歌词和外挂歌词，并根据内容特征辅助判断格式，不得只依赖文件扩展名。
- 对同一组歌词，如果下行与上行起始时间相同，或起止时间相同，则优先将下行识别为上行翻译。
- 解析失败时保留可显示的纯文本，不得因为歌词格式错误影响歌曲播放。

## 实施基线与长期计划

以下内容来源于项目整体规划，作为后续开发的长期技术基线。已经完成的功能不能被
误认为仍待实现；尚未完成的阶段必须按本节约束推进。

### 总体取舍

- 采用 mpv 与原生 DSP 的混合架构。
- 先稳定基础播放、曲库和歌词，再接入高级 DSP。
- 空间化以耳机双声道为首要目标。
- 不提供独立音量滑块。
- ReplayGain 默认可选启用，优先曲目增益，缺失时回退专辑增益，并启用削波保护。
- 播放次数在累计播放达到 `min(30 秒, 歌曲时长 50%)` 后记录；未知时长使用 30 秒阈值；一次播放会话只记录一次。
- 所有 Git 命令由用户执行。

### 播放与音频处理架构

播放层应逐步收敛为以下职责边界：

- `PlaybackEngine`：定义打开、播放、暂停、跳转、定位、释放和状态事件。
- `MpvPlaybackEngine`：使用 `media_kit/libmpv`，负责基础播放、ReplayGain 和基础滤镜。
- `NativeDspPlaybackEngine`：后续 Linux 原生音频后端，计划使用 Rust + FFI，负责高级 DSP。
- `PlaybackCoordinator`：根据音频设置选择后端；原生后端不可用或格式不支持时回退 mpv，并显示非阻塞警告。

后端切换必须保存当前曲目、播放位置和播放状态，重新打开后恢复位置，不能打断用户操作。

统一音频设置模型至少包含：

- `normalizationEnabled`
- `equalizerEnabled`
- `preampDb`
- `equalizerBands`
- `bassEnhancementSettings`
- `spatialSettings`
- `limiterEnabled`
- `processingBackend`

高级 DSP 使用 32 位浮点音频块处理，设计目标包括：

- EQ：参数化双二阶滤波器级联；
- Bass：低频分离、动态谐波生成、混合和安全限制器；
- 耳机空间化：先实现可调 Crossfeed，再支持用户导入 SOFA HRTF；
- 输出保护：统一峰值限制器和输出余量；
- 多声道上混：保留接口，但不列入第一阶段验收。

复杂算法必须在 `docs/dsp-sources.md` 中记录参考项目、版本、许可证和改动范围，
不得复制许可证不兼容的代码。当前阶段仅使用 mpv/libavfilter 完成基础处理，HRTF、
SOFA、动态 Bass 谐波增强和多声道上混不得伪装成已经完成的功能。

### 曲库、元数据与设置

- 保留标题、艺术家、专辑、封面、时长、采样率、比特率和所有原始标签。
- 对 Opus/Vorbis 的 `LYRICS`、`LYRIC`、`SYLT`、`USLT`、`UNSYNCEDLYRICS` 等字段逐项读取，不能把候选歌词直接拼接为一段原文。
- 使用 `LyricsSource` 保存内容、来源类型、扩展名、标签名、语言和角色。
- 外挂歌词优先于内嵌歌词；同名外挂歌词按 LRC、Enhanced LRC、QRC、YRC、KRC、TTML、ASS、SRT、VTT、TXT 顺序处理。
- `Track` 保留 `lyrics`、`lyricsFormat` 兼容字段，同时保存所有候选歌词。
- 歌曲来源必须区分 XDG 音乐目录和用户添加目录，路径 canonical 化后去重。
- 设置使用 Drift 配置表，以版本化 JSON 保存音频处理参数。
- 数据库迁移必须保留既有播放统计和历史事件。

播放统计采用播放会话模型：播放成功后开始计时，达到阈值后写入一条
`PlaybackEvents`；暂停不清零，切换曲目或重新开始同一曲目创建新会话；年度总结读取事件表，支持年度总播放次数、曲目排名和按年份筛选。

### 歌词解析扩展要求

当前顶层解析函数后续可按需要整理为可测试的解析器集合：

- `LrcParser`
- `QrcParser`
- `YrcParser`
- `KrcParser`
- `TtmlParser`
- `SubtitleParser`
- `AssParser`
- `PlainTextParser`

所有格式统一输出 `UntimedLyricsData`、`LineTimedLyricsData` 和
`WordTimedLyricsData`，并保留原文、翻译、对唱、说话者、语言、附加变体、行起止时间、逐字起止时间、语法格式和时间戳格式。

翻译合并规则：

- 下行与上行起始时间相同，识别为翻译；
- 两行起止时间相同，识别为翻译；
- 不同起点但结束时间相同，也识别为翻译；
- 同一时间存在第三行时保留为附加变体；
- 保持原始顺序，不能依赖不稳定排序；
- 解析失败时降级为纯文本，不能影响播放。

### 悬浮播放器与播放页

- 未悬停时只有专辑封面圆参与命中测试。
- 悬停后，专辑圆、圆与菜单之间的透明桥接区域、两个菜单共同参与命中测试。
- 控制菜单自动向上或向下弹出，歌词菜单自动向左或向右弹出。
- 两个菜单从圆心位置、圆形尺寸和裁切状态同时插值到目标位置和尺寸。
- 拖动期间菜单保持展开尺寸，不改变透明度，并与专辑圆同步移动；拖动结束后继续保持展开，直到整个交互簇离开命中区域。
- 进度环从圆顶端开始，未播放部分半透明；悬停时半径大于封面圆，并显示可拖动手柄。
- 歌词菜单只渲染统一歌词模型，不显示原始时间戳；逐字歌词按绝对播放时间高亮；无时间歌词显示“歌词没有时间信息”。
- 歌词菜单采用歌曲标题和歌手、原文歌词、翻译歌词的三行结构。
- 音轨详情弹窗关闭只能关闭当前对话框，不残留全窗口遮罩。
- 列表项封面统一使用圆形裁切。
- 左侧导航不放置“歌词”和“音频设置”，音频设置统一放入设置页。
- “歌曲来源”页展示 XDG 音乐目录、用户添加目录、扫描状态和目录曲目数量。
- 点击专辑圆进入播放页时，使用同一个圆形裁切路径从封面实际位置扩展到全窗口；不能用先显示全屏页面再改变透明度的伪过渡。
- 播放页内部布局保持可扩展，具体视觉布局另行设计。

### 分阶段实施

1. 基础稳定化：修复工具链，持久化音频设置，完善播放状态、错误处理、切歌、定位和统计，修正曲库来源、封面、详情弹窗和数据库迁移。
2. 歌词与曲库可靠性：完善歌词来源选择、格式解析、翻译合并、逐字高亮、真实音频样例和音轨详情。
3. 悬浮播放交互：完善命中区域、菜单几何动画、拖动同步、进度环手柄、圆形全屏揭示和小窗口边缘行为。
4. 基础音频处理：接入 ReplayGain、mpv EQ、预放大、重置和预设，并增加滤镜参数和频响测试。
5. 高级原生 DSP：建立 Rust DSP 库和 Dart FFI，实现 EQ、动态 Bass、Crossfeed、限幅器、后端切换、定位恢复和 mpv 回退，后续加入 SOFA HRTF 与多声道上混接口。

### 测试与验收

测试应覆盖：

- 歌词格式识别、时间单位、逐字高亮、翻译、对唱、损坏输入和无时间歌词；
- Opus/Vorbis 歌词标签、封面、ReplayGain 和外挂歌词优先级；
- 数据库迁移、播放统计保留、设置序列化和年度统计；
- 播放/暂停/切歌/定位、ReplayGain 开关、后端回退和播放次数阈值；
- EQ 增益方向、频率响应、Bass 输出限制、立体声通道和削波保护；
- 圆形命中区域、菜单桥接、拖动展开、进度环起点、详情弹窗关闭和圆形封面；
- Linux 真实 Opus 逐字歌词、外挂翻译歌词、无时间歌词、窗口边缘、最小窗口尺寸和圆形揭示动画。

最终验证命令为 `dart analyze`、`flutter test` 和 `flutter build linux`。若工具链或权限导致失败，必须报告实际错误，不能将未验证内容描述为已通过。

### 默认假设

- 当前开发和第一版发布目标仅为 Linux，其他平台只保留领域层和接口层的可移植性。
- 不依赖在线歌词服务、在线音频服务或系统全局音量控制。
- 不在第一阶段实现多声道上混。
- 原生 DSP 不可用时必须保证 mpv 基础播放仍可用。
- 新增 Rust/FFI 构建链前，先完成最小音频块处理验证，再扩大到完整原生播放后端。

## 当前功能状态（2026 年 9 月 26 日）

本节是代码现状清单，优先级高于早期计划中的笼统描述。新增功能或修复问题后，
应同步更新本节。

### 已实现

- Flutter/Riverpod Linux 优先应用壳、曲库、设置、年度总结和浮动播放器入口。
- MiSans 可变字体随应用打包，并通过根 Material 主题应用于全局 Flutter 文本；AMLL 自绘歌词也显式使用同一字体族。
- Drift 曲库数据库、应用设置表、播放事件表，以及从旧版本迁移到 schema 8 的迁移逻辑。
- XDG 音乐目录读取、用户选择目录、路径规范化去重、递归音频扫描和扫描状态展示。
- MP3、FLAC、M4A、AAC、OGG、Opus、WAV、AIFF、OGA 和 WebM 文件的扫描尝试；损坏文件会跳过，不中断整库扫描。
- 标题、艺术家、专辑、时长、采样率、比特率、曲目号、封面、ReplayGain 和原始标签详情读取；Ogg/Opus 时长按容器粒度位置和 Opus 固定 48 kHz 时钟精确计算，避免输入采样率或 `LENGTH` 标签造成偏差。
- 内嵌封面优先使用正面封面，曲库列表、专辑列表和艺术家列表使用圆形裁切。
- 基于 `media_kit/libmpv` 的打开、播放、暂停、切歌和定位；定位输入会限制在有效范围内。
- ReplayGain 曲目增益优先、专辑增益回退、削波保护和可选响度均衡；没有独立应用音量滑块。
- mpv/libavfilter 基础处理链：预放大、十段参数化 EQ、基础 Bass、Crossfeed 和输出限幅。
- 音频设置的 JSON 持久化、重置、EQ/Bass/Crossfeed/ReplayGain/限幅控制。
- 播放会话统计：达到 `min(30 秒, 时长 50%)` 后记录一次；未知时长使用 30 秒阈值；年度播放次数汇总。
- LRC、Enhanced LRC（尖括号及方括号逐字格式）、QRC、YRC、明文 KRC、TTML、SRT、VTT、ASS/SSA 和纯文本歌词识别。
- 无时间戳、逐行时间戳、逐字时间戳统一模型；支持翻译、语言、说话者、对唱变体、行/字起止时间和外挂/内嵌来源。
- 同起始时间、同结束时间或同起止时间的下行歌词翻译合并；匹配可跨过不相关行，额外译文保留为变体；独立翻译来源优先同时匹配起止时间，单条译文不会被重复消费，纯文本译文可按行号关联带时间原文。
- TTML 时间按父级偏移逐层解析，支持 `par`/`seq` 时间容器、`ms`/`s`/`m`/`h`/`f`/`t` 以及帧时钟；遵循 `xml:space="preserve"`，显式翻译角色不依赖 XML 中原文和译文的排列顺序。
- 歌词菜单显示解析后的原文、翻译和逐字高亮；音轨详情显示纯歌词、语法格式和时间戳格式。
- 圆形专辑封面悬浮播放器、顶部起始进度环、悬停手柄、上下/左右菜单、菜单桥接命中区域、圆形揭示过渡，以及按窗口中线决定展开方向和按归一化坐标持久化拖动位置；拖动进度时本地预览，释放后提交一次定位。
- 第一种 AMLL 风格播放页：封面采样背景与缓动光感、横竖屏自适应、专辑封面和歌曲信息、音质标签、进度定位、播放/暂停/切歌、随机与循环、歌词显隐和可选曲播放队列；专辑封面播放和暂停时保持相同尺寸。
- AMLL 风格歌词视图：主歌词左对齐并使用当前配置的 450 字重，字号按 AMLL 的 `5vh/2.5vw` 视口规则响应；歌词行水平内边距与顶部 `0.4em` 相同，底部额外增加 4px，行组采用 `0.3em` 副行间距、当前配置的 `0.1` 字距，歌词列表隐藏滚动条并在行悬停时显示圆角淡背景；活动行按用户要求锚定视口三分之一处，具有 97% 非活动行缩放、FLIP 归位与约 45ms 累计错峰，活动行延迟归零且上方行约提前 36ms 进入预滚动、远近透明度/模糊过渡、按实际字框高度一半计算、移动距离额外包含一个羽化宽度的连续逐词高亮、固定排版下的 AMLL 普通逐词上浮与条件式字符强调、按 AMLL 可展示性规则插入和移除的前奏/间奏三点动画、悬停渐隐模糊，以及正文 `4/5` 字号翻译和独立计时的对唱背景行。三点使用与歌词正文一致的四向内边距；间奏占位插入/移除时，受影响歌词行沿同一时钟按错峰动画归位或填补空间。滚轮采用短时缓动并放大步长，用户滚动后 3 秒内冻结自动定位；歌词点击或进度定位同步移动，长跨度定位通过真实滚动逐步构建懒加载的中间行，定位期间过滤方向相反的旧播放位置回报，避免歌词高亮回退。所有活动歌词词片段均按 AMLL 规则普通上浮；仅满足 `shouldEmphasize` 的空格/CJK 分组才额外启用字符级缩放、推挤、辉光和正弦上浮，辉光源使用独立的单字符栅格遮罩，避免相邻字形进入模糊层。逐字绘制缓存已排版的 `TextPainter`、同源词片段及词/字边界，动画帧仅更新绘制状态，并通过局部 `RepaintBoundary` 隔离重绘；透明 `Text` 子组件仍负责原有尺寸与布局。行高测量与歌词绘制共用系统字重、字距、语言环境、文字缩放和行高行为，另外保留 2px 渲染舍入余量，避免长歌词/译文换行后溢出。
- Enhanced LRC 解析会保留逐字时间标签之间的纯空格片段，中文歌词中用于分隔词/字的空格不会因空片段过滤而丢失；纯空格不会被误建成可高亮文字词。
- AMLL 专辑封面在播放和暂停时保持相同尺寸；切歌后加载歌词时立即聚焦首句。
- AMLL 参考来源、0.6.0 版本和 AGPL-3.0-only 许可证边界记录于 `docs/playback-style-sources.md`；Flutter 实现没有复制其代码或资源。
- 专辑、艺术家和当前播放队列的基础浏览页面；歌曲来源页显示目录类型、扫描状态和曲目数量。
- 76 项歌词、音频处理、数据库、播放器状态和 Widget 回归测试；截至 2026 年 9 月 26 日，本轮 `dart analyze`、完整 `flutter test` 和 `flutter build linux` 均已通过。Opus 时长回归使用合成 Ogg/Opus 页面验证粒度位置换算。

### 部分实现或存在边界

- `LibrarySource` 尚未成为独立领域模型；歌曲来源页目前根据目录路径后缀推断 XDG 音乐目录，不能完整表达所有自定义 `user-dirs.dirs` 配置。
- `LibraryScanner` 仍在异步方法中串行读取元数据，没有使用 isolate 或增量索引；大型曲库首次扫描可能占用界面线程。
- ReplayGain 已覆盖 Vorbis/Opus、MP3 和 APE 常见标签；MP4/M4A 的 `iTunNORM` 等专有响度标签尚未适配。
- QRC/YRC 的逐字时间已按绝对毫秒解析，但加密 QRC/KRC 和元数据包装层尚未解密；KRC 当前只接受明文内容。
- 第一种播放页及歌词动画已参照 AMLL 的布局、时间线与动效参数实现，普通词浮动、`shouldEmphasize` 判定、词组边界和字符动画调度已对齐 AMLL；Flutter 字体度量和 Canvas 辉光合成效果尚未通过真实 Linux 截图逐像素验收。注音/罗马音和完整的背景行/歌词组弹簧时序仍未实现；手势滚动期间的焦点挂起、3 秒后恢复自动跟随以及定位时同步滚动已实现。
- 随机/循环模式当前为播放会话内状态，没有持久化；音质标签只展示本地文件格式和扫描到的采样率，不代表经验证的 Hi-Res 或空间音频认证。
- AMLL 播放页样式由 Flutter 独立重写；没有复用其 AGPL 源码或资源。若后续改为复制/改编上游代码，必须重新评估许可证义务。
- 悬浮播放器已按圆心所在窗口半区决定菜单方向，菜单尺寸按内容自适应；最小窗口、高 DPI、Wayland 多屏和真实鼠标拖动仍需要 Linux 手工验收。
- 年度总结目前提供年度总次数和累计次数，不提供年度曲目排名、按年份筛选或听歌时长统计。
- 当前播放器直接使用 `Player` 和 `NativePlayer`，尚未抽出 `PlaybackEngine`、`PlaybackCoordinator` 和后端切换状态机。
- `BassEnhancementSettings.harmonics` 已进入统一设置模型，但 mpv 后端只使用基础 Bass 滤镜，尚未实现动态谐波增强。

### 尚未实现

- Rust + FFI 原生 DSP 后端、32 位浮点音频块处理和原生后端故障回退。
- 真正的双二阶原生 EQ、动态智能 Bass、SOFA HRTF、可导入 HRTF 文件和多声道上混。
- `processingBackend` 设置、mpv/原生后端切换时的位置恢复和非阻塞能力警告。
- 播放列表持久化、队列编辑、收藏、排序、筛选、专辑/艺术家详情页和年度曲目排名。
- 网络歌词或音频服务；这是产品约束，不应为了补齐功能而引入在线依赖。
- 真实 Opus/Vorbis 音频样例、数据库旧 schema 迁移样例、DSP 频响/削波数学测试和 Linux 自动化视觉测试。
- 高级 DSP 引入前的上游项目、固定版本、许可证和改动记录；目前仅在 `docs/dsp-sources.md` 记录“尚未复制第三方源码”。

## 验证与报告

- 完成修改后先运行静态分析和针对性测试，再报告结果。
- 如果工具链、权限或环境导致验证失败，必须说明实际执行的命令、具体错误和未验证的部分。
- 不要声称执行过未实际执行的命令，也不要声称测试通过而省略环境错误。
