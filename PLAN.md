• Proposed Plan


  # 伶伦项目整体重规划

  ## 摘要

  当前项目已经具备 Flutter/Riverpod 基础架构、Drift 曲库数据库、media_kit/libmpv 播放、元数据扫描、封面显示、播
  放统计和歌词解析雏形。后续目标是将其整理为 Linux 优先、跨平台可扩展的本地音乐播放器。

  已确定的底层取舍：

  - 采用 mpv 与原生 DSP 的混合架构。
  - 分阶段交付，先稳定基础播放和曲库，再接入高级 DSP。
  - 空间化以耳机双声道为首要目标。
  - 不提供独立音量滑块。
  - ReplayGain 默认可选启用，曲目增益优先，缺失时回退专辑增益，并启用削波保护。
  - 播放次数在累计播放达到 min(30 秒, 歌曲时长 50%) 后记录，一次播放会话只记录一次。
  - 所有 Git 命令由用户执行。

  ## 核心架构

  ### 播放与音频处理

  将 PlayerController 改为只管理播放状态和队列，实际播放交给统一接口：

  - PlaybackEngine：定义打开、播放、暂停、跳转、定位、释放、状态事件。
  - MpvPlaybackEngine：继续使用现有 media_kit/libmpv，负责基础播放、ReplayGain 和基础滤镜。
  - NativeDspPlaybackEngine：Linux 原生音频后端，使用 Rust + FFI，负责高级 DSP；解码使用 Rust 音频解码库，输出
    使用 Linux 音频设备。

  - PlaybackCoordinator：根据当前音频设置选择后端。高级 Bass 或空间化启用时切换到原生后端；原生后端不可用或格式
    不支持时自动回退 mpv，并向设置页显示非阻塞警告。

  切换后端时保存当前曲目、播放位置和播放状态，重新打开后定位到原位置，避免影响用户操作。

  新增统一音频设置模型：

  - normalizationEnabled
  - equalizerEnabled
  - preampDb
  - equalizerBands
  - bassEnhancementSettings
  - spatialSettings
  - limiterEnabled
  - processingBackend

  基础 EQ 使用双二阶滤波器，支持 10 段图形均衡器和高级参数化设置。每个频段包含频率、增益、Q 值和滤波器类型。

  高级 DSP 使用 32 位浮点音频块处理：

  - EQ：参数化双二阶滤波器级联。
  - Bass：低频分离、动态谐波生成、混合和安全限制器。
  - 耳机空间化：先实现可调 Crossfeed，再支持用户导入 SOFA HRTF。
  - 输出保护：统一峰值限制器和输出余量。
  - 多声道上混保留接口，但不列入第一阶段验收。

  复杂算法必须建立 docs/dsp-sources.md，记录参考项目、算法来源、许可证、版本和自行改动；不得直接复制许可证不兼
  容的代码。

  ### 曲库、元数据与设置

  保留现有 audio_metadata_reader、Drift 和 XDG 音乐目录扫描逻辑，新增统一适配层：

  - 保留标题、艺术家、专辑、封面、时长、采样率、比特率和所有原始标签。
  - 对 Opus/Vorbis 的 LYRICS、LYRIC、SYLT、USLT、UNSYNCEDLYRICS 等字段逐项读取，不再把多个候选歌词直接拼接成一
    段原文。

  - 新增 LyricsSource，记录歌词内容、来源类型、文件扩展名、标签名、语言和角色。
  - 外挂歌词优先于内嵌歌词；同名外挂文件按 LRC、Enhanced LRC、QRC、YRC、KRC、TTML、ASS、SRT、VTT、TXT 顺序选
    择。

  - 曲目保留当前 lyrics、lyricsFormat 兼容字段，同时保存所有候选歌词供音轨详情查看。
  - LibrarySource 明确区分 XDG 音乐目录和用户添加目录，路径 canonical 化后去重。
  - 设置写入新的 Drift 配置表，使用版本化 JSON 保存音频处理设置。
  - 数据库迁移至少新增设置表和歌词来源数据列，保留现有播放统计及历史事件。

  播放统计改为播放会话模型：

  - 播放成功后开始计时。
  - 达到 min(30 秒, 时长 50%) 时写入一条 PlaybackEvents。
  - 暂停不清零，切换曲目或重新开始同一曲目创建新会话。
  - 年度总结读取事件表，提供年度总播放次数、曲目排名和按年份筛选。

  ### 歌词解析

  将当前顶层函数整理为可测试的 LyricsParser 和格式解析器集合：

  - LrcParser
  - QrcParser
  - YrcParser
  - KrcParser
  - TtmlParser
  - SubtitleParser
  - AssParser
  - PlainTextParser

  所有格式统一输出：

  - UntimedLyricsData
  - LineTimedLyricsData
  - WordTimedLyricsData

  统一模型继续支持：

  - 原文
  - 翻译
  - 对唱和说话者
  - 语言
  - 附加变体
  - 行起止时间
  - 逐字起止时间
  - 格式和时间戳格式标签

  翻译合并规则固定为：

  - 下行与上行起始时间相同，识别为翻译。
  - 两行起止时间相同，识别为翻译。
  - 不同起点但结束时间相同，也识别为翻译。
  - 同一时间存在第三行时保留为附加变体。
  - 保持原始顺序，不能依赖不稳定排序。
  - 解析失败时降级为纯文本，不影响播放。

  需要覆盖普通 LRC、Enhanced LRC、QRC、YRC、明文和加密 KRC、TTML、SRT、VTT、ASS，以及混合翻译和对唱样例。

  ### 悬浮播放器与播放页

  保留当前圆形专辑封面作为唯一常驻播放入口，重做交互状态机：

  - 未悬停时只有专辑封面圆本身参与命中测试。
  - 悬停后，专辑圆、圆与菜单之间的透明桥接区域、两个菜单共同参与命中测试。
  - 控制菜单自动向上或向下弹出。
  - 歌词菜单自动向左或向右弹出。
  - 两个菜单从圆心位置、圆形尺寸和裁切状态同时插值到目标位置和目标尺寸。
  - 拖动期间菜单保持当前展开尺寸，不回到悬停前的圆形尺寸，不改变透明度，并与专辑圆同步移动。
  - 拖动结束后维持展开状态，直到整个交互簇离开命中区域。
  - 进度环从圆顶端开始，未播放部分半透明，悬停时半径大于封面圆，并显示可拖动手柄。
  - 歌词菜单只渲染解析后的歌词模型，不再显示原始时间戳。
  - 逐字歌词根据统一的绝对播放时间高亮；无时间歌词显示“歌词没有时间信息”。
  - 歌词菜单固定为三行结构：歌曲标题和歌手、原文歌词、翻译歌词。
  - 音轨详情弹窗关闭必须只关闭当前对话框，不残留全窗口遮罩。
  - 专辑封面列表项统一使用圆形裁切。
  - 左侧导航不再放置“歌词”和“音频设置”；音频设置统一放入设置页。
  - “歌曲来源”展示 XDG 音乐目录、用户添加目录、扫描状态和目录中的曲目数量。
  - 点击专辑圆进入播放页时，使用同一个圆形裁切路径从封面实际位置扩展到全窗口；背景、封面和播放页内容都必须位于
    该裁切区域内，不能通过先显示全屏页面再做透明度伪装。

  - 播放页内部布局暂时只建立可扩展容器，具体视觉布局另行设计。

  ## 分阶段实施

  1. 基础稳定化
      - 修正 Flutter SDK 缓存问题，恢复 flutter test 和 flutter build linux。
      - 抽离播放引擎接口。
      - 持久化音频设置。
      - 完善播放错误、切歌、定位和播放统计状态。
      - 修正曲库来源、封面、详情弹窗和数据库迁移。

  2. 歌词与曲库可靠性
      - 重构歌词来源选择和元数据候选提取。
      - 完成格式解析器、翻译合并和逐字高亮。
      - 用真实 Opus、Vorbis、外挂 LRC、TTML、QRC/YRC/KRC 文件建立固定测试样例。
      - 完善音轨详情中的解析格式、时间格式和纯歌词展示。

  3. 悬浮播放交互
      - 重做命中区域、菜单几何动画、拖动同步和进度环手柄。
      - 实现严格圆形扩展到全屏的播放页过渡。
      - 验证小窗口、窗口边缘、上下左右弹出和高 DPI 场景。

  4. 基础音频处理
      - 接入 ReplayGain 标准响度播放和削波保护。
      - 实现 mpv 基础 EQ 滤镜图。
      - 设置页加入 10 段 EQ、预放大、重置和预设。
      - 增加滤镜参数计算与频响测试。

  5. 高级原生 DSP
      - 建立 Rust 原生 DSP 库和 Dart FFI 接口。
      - 实现 EQ、动态 Bass、Crossfeed、限幅器。
      - 实现原生后端切换、定位恢复和 mpv 回退。
      - 后续加入 SOFA HRTF 导入和多声道上混接口。

  ## 测试与验收

  - 歌词解析单测：格式识别、时间单位、逐字高亮、翻译规则、对唱、损坏输入和无时间歌词。
  - 元数据单测：Opus/Vorbis 多种歌词标签、封面、ReplayGain、外挂歌词优先级。
  - 数据库测试：旧 schema 迁移、播放统计保留、设置序列化、年度统计。
  - 播放器测试：播放/暂停/切歌/定位、ReplayGain 开关、后端回退、播放次数阈值。
  - DSP 数学测试：滤波器增益方向、频率响应、Bass 输出限制、立体声通道、削波保护。
  - Widget 测试：圆形命中区域、菜单桥接、拖动时菜单保持展开、进度环起点、详情弹窗关闭和圆形封面。
  - Linux 手工验收：真实 Opus 逐字歌词、外挂翻译歌词、无时间歌词、窗口边缘交互、最小窗口尺寸和播放页圆形揭示动
    画。

  - 最终验证命令为 dart analyze、flutter test 和 flutter build linux；当前 dart analyze 已通过，Flutter 命令仍
    受本机 SDK 缓存缺失/只读问题影响。

  ## 默认假设

  - 当前开发和第一版发布目标仅为 Linux，其他平台只保留领域层和接口层的可移植性。
  - 不依赖在线歌词服务、在线音频服务或系统全局音量控制。
  - 不在第一阶段实现多声道上混。
  - 原生 DSP 不可用时必须保证 mpv 基础播放仍可用。
  - 新增 Rust/FFI 构建链前，先完成最小音频块处理验证，再扩大到完整原生播放后端。
